class DetailEvent{
  String title;
  String type;
  String body;
  String date;
  String thoroughfare;
  String postalcode;
  var additionalInfo;
  DetailEvent(this.title, this.type, this.body, this.date, this.thoroughfare, this.postalcode, this.additionalInfo);
}